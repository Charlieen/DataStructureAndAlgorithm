/**
 * Create a function that reverse a string hi my name is ding
 * should be gnid si eman ih 
 */
import MyArray from './myArray';

 function reverse(str) {

    const array = new MyArray();
    console.log(array.fromStrings(str));
 }

 reverse('hi my name is Charlie Ding');